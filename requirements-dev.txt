# Development dependencies
-r requirements.txt

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
psutil>=5.9.0

# Code quality
# Pinned to the 25.x stable-style series: line-length pins the width, not the
# style, and Black's stable style changes at calendar-year releases. Note 25.12
# dropped Python 3.9, so the 3.9 matrix legs resolve to 25.11 -- verified to
# produce byte-identical output. Moving to 26.x requires dropping Python 3.9
# first, and reformatting the tree.
black>=25.1,<26
flake8>=6.1.0
mypy>=1.5.1
types-PyYAML>=6.0.0
isort>=5.12.0

# Documentation
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0

# Build tools
build>=0.10.0
twine>=4.0.2